<%= form_for @game, :html => { :multipart => true } do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= file_field_tag "game[image]" %>
  </div>
  <div class="field">
    <%= f.label :genre_ids %><br>
    <%= f.text_field :genre_ids %>
  </div>
  <div class="field">
    <%= f.label :platform_ids %><br>
    <%= f.text_field :platform_ids %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
